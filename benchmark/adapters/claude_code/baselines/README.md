# Claude Code Live Evaluation Baselines

This directory records selected live Claude Code adapter evaluation runs as
evidence.

These files are not deterministic golden outputs and are not used as CI
expectations. Live Claude behavior may vary between runs and model or CLI
versions. Deterministic repository validation remains in the unit tests,
structural validation, and benchmark suites.
